import authSlice from './auth/slices/auth.slice';
import walletSlice from './wallet/wallet.slice';
import cartSlice from './auth/slices/cart.slice';
export default {
  authSlice,
  walletSlice,
  cartSlice,
};
